{
  "name": "productivity-suite-shared-library",
  "description": "library containing shared logic used by the productivity suite app and its fragments",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc && vite build",
    "build.watch": "tsc && vite build -w"
  },
  "dependencies": {
	"cookie": "^0.5.0"
  },
  "devDependencies": {
    "@types/cookie": "^0.5.1",
	"typescript": "^4.6.4",
    "vite": "^3.1.0",
    "vite-plugin-dts": "^1.4.1"
  }
}
